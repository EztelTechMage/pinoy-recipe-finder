// Utility function to handle image paths
export const getImagePath = (imageName) => {
  // Check if the image exists in public folder
  // This is a simple implementation - in a real app you might want to check if the file exists
  return `/${imageName}`;
};

// Utility function to format cooking time
export const formatTime = (timeString) => {
  return timeString;
};